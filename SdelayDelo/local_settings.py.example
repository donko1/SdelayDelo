from .settings import BASE_DIR
from os import path

with open(path.abspath("./SdelayDelo/secret_key.txt"), "r", encoding="utf-8") as f:
    SECRET_KEY = f.read().strip()

ALLOWED_HOSTS = []

DATABASES = {
    "default": {
        "ENGINE": "django.db.backends.sqlite3",
        "NAME": BASE_DIR / "db.sqlite3",
    }
}

EMAIL_HOST = "smtp.server.ru"
EMAIL_PORT = "ur port"
EMAIL_USE_TLS = True
EMAIL_USE_SSL = True


EMAIL_HOST_USER = "example@example.ru"
EMAIL_HOST_PASSWORD = "qwerty123"

DEFAULT_FROM_EMAIL = EMAIL_HOST_USER
SERVER_EMAIL = EMAIL_HOST_USER
EMAIL_ADMIN = EMAIL_HOST_USER

EMAIL_EXISTS = False

LANGUAGE_CODE = 'ru'
USE_I18N = True
USE_L10N = True
USE_TZ = True